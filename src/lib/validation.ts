// Validation helpers
// TODO: реализовать логику

export const validateMedicine = (data: any) => {
  // Placeholder
  return true
}

export const sanitizeMedicineName = (name: string) => {
  return name.trim()
}
