name: Android verification

on:
  push:
    branches:
      - fix/android-ready-20260719-working
      - rebuild/po-chasam-2
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Install dependencies
        run: npm install

      - name: TypeScript check
        shell: bash
        run: |
          set +e
          npm run type-check > typecheck.log 2>&1
          status=$?
          cat typecheck.log
          exit $status

      - name: Upload TypeScript diagnostics
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: pills-05-typecheck-log
          path: typecheck.log
          if-no-files-found: ignore

      - name: Export web application
        shell: bash
        run: |
          set +e
          npm run build > web-build.log 2>&1
          status=$?
          cat web-build.log
          exit $status

      - name: Upload web build diagnostics
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: pills-05-web-build-log
          path: web-build.log
          if-no-files-found: ignore

      - name: Sync Capacitor Android
        shell: bash
        run: |
          set +e
          npx cap sync android > capacitor-sync.log 2>&1
          status=$?
          cat capacitor-sync.log
          exit $status

      - name: Upload Capacitor diagnostics
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: pills-05-capacitor-log
          path: capacitor-sync.log
          if-no-files-found: ignore

      - name: Build debug APK
        working-directory: android
        shell: bash
        run: |
          chmod +x gradlew
          set +e
          ./gradlew assembleDebug > ../gradle-build.log 2>&1
          status=$?
          tail -n 120 ../gradle-build.log
          exit $status

      - name: Verify and persist blister-photo APK
        if: github.event_name == 'pull_request' && github.head_ref == 'feature/blister-photo-20260828'
        shell: bash
        env:
          HEAD_BRANCH: ${{ github.head_ref }}
        run: |
          set -euo pipefail
          apk=android/app/build/outputs/apk/debug/app-debug.apk
          test -f "$apk"
          sha256sum "$apk" | tee Pills-blister-photo-20260828-sha256.txt
          mkdir -p /tmp/pills-blister-photo
          cp "$apk" /tmp/pills-blister-photo/Pills-blister-photo-20260828.apk
          cp Pills-blister-photo-20260828-sha256.txt /tmp/pills-blister-photo/Pills-blister-photo-20260828-sha256.txt
          git fetch origin "$HEAD_BRANCH"
          git checkout -B "$HEAD_BRANCH" "origin/$HEAD_BRANCH"
          mkdir -p preview
          cp /tmp/pills-blister-photo/Pills-blister-photo-20260828.apk preview/Pills-blister-photo-20260828.apk
          cp /tmp/pills-blister-photo/Pills-blister-photo-20260828-sha256.txt preview/Pills-blister-photo-20260828-sha256.txt
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -f preview/Pills-blister-photo-20260828.apk preview/Pills-blister-photo-20260828-sha256.txt
          git commit -m 'ci: attach Pills blister-photo APK [skip ci]' || exit 0
          git push origin HEAD:"$HEAD_BRANCH"

      - name: Upload Gradle diagnostics
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: pills-05-gradle-log
          path: gradle-build.log
          if-no-files-found: ignore

      - name: Upload debug APK
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: pills-05-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error