# Правила разработки для ИИ агентов

Этот документ определяет неподвижные правила для всех работ с этим проектом.

## 🚫 ЗАПРЕТЫ (АБСОЛЮТНЫЕ)

### Структура проекта
- ❌ НЕ менять структуру папок в `src/`
- ❌ НЕ объединять файлы
- ❌ НЕ писать весь код в одном месте
- ❌ НЕ переименовывать основные папки

### Функциональность
- ❌ НЕ добавлять новые функции сверх требований
- ❌ НЕ ломать напоминания
- ❌ НЕ удалять проверки и валидацию
- ❌ НЕ менять UX без одобрения

### Интерфейс
- ❌ НЕ менять тексты интерфейса (кроме ошибок и логирования)
- ❌ НЕ усложнять UI
- ❌ НЕ делать скрытые кнопки или невидимые элементы

## 🔘 ТРЕБОВАНИЯ К КНОПКАМ

**КАЖДАЯ кнопка обязательно:**

- ✅ Меняет цвет при нажатии
- ✅ Показывает текст или иконку
- ✅ Выполняет одно действие
- ✅ Дает мгновенный ответ интерфейсу

**ЗАПРЕЩЕНО:**
- ❌ Кнопка без реакции на нажатие
- ❌ Молчание интерфейса более 100ms
- ❌ Невидимые кнопки
- ❌ Кнопки, которые иногда работают

## 📢 НАПОМИНАНИЯ (ОБЯЗАТЕЛЬНЫЕ ЭЛЕМЕНТЫ)

### Каналы уведомлений

Каждое напоминание ДОЛЖНО содержать:

1. **Экран** — большое, яркое напоминание
2. **Звук** — встроенный звук (mp3/wav/ogg)
3. **Голос** — синтез речи: **"Время принимать таблетки"** (ТОЧНЫЙ текст)
4. **Push-уведомление** — системное уведомление

### Повторения напоминаний

```
Первое напоминание → 5 минут ждём → Второе напоминание
                  → 10 минут ждём → Третье напоминание
                  → СТОП (больше нет)
```

⚠️ **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО:**
- ❌ Бесконечные повторения
- ❌ Более 3 напоминаний подряд
- ❌ Меньше 5 минут между повторами

## 🔊 ГОЛОС

### Синтез речи

- **ВСЕГДА** использовать Web Speech API (SpeechSynthesis)
- **ВСЕГДА** говорить текст: **"Время принимать таблетки"**
- **НЕЛЬЗЯ** менять текст

### Параметры голоса

- Язык: `ru-RU` (русский)
- Скорость: 1.0 (нормальная)
- Громкость: 1.0 (максимальная)

## 🎵 ЗВУК

### Форматы
- ✅ MP3
- ✅ WAV
- ✅ OGG
- ❌ Больше ничего

### Источники
1. **Встроенные звуки** — в приложении (обязательно минимум 1)
2. **Звук пользователя** — можно загружать свой файл

## 💾 ДАННЫЕ

### Требования целостности

- ❌ НЕ допускать дубликатов лекарств
- ❌ НЕ создавать orphan данные (записи без хозяина)
- ❌ НЕ хранить старые уведомления (удалять через день)
- ✅ ВСЕГДА очищать устаревшие данные

### Хранилище

- Использовать **localStorage** (встроенное в браузер)
- Структурировать данные по типам
- Версионировать структуру при изменениях

## 🏗 АРХИТЕКТУРА

### Разделение ответственности

```
src/
├── components/     → ТОЛЬКО UI компоненты
├── features/       → Бизнес-логика
├── store/          → Управление состоянием (Zustand)
├── lib/            → Утилиты и хелперы
└── types/          → TypeScript типы
```

### СТРОГИЕ ПРАВИЛА

- ❌ НЕ смешивать логику и UI
- ❌ НЕ писать бизнес-логику в компонентах
- ❌ НЕ хранить состояние в React state для глобальных данных
- ✅ ИСПОЛЬЗУЙТЕ Zustand для глобального состояния
- ✅ ОТДЕЛЯЙТЕ логику в `features/`

## 🧪 ПРОВЕРКИ (КРИТИЧНЫЕ)

### Перед каждым коммитом

- ✅ Все кнопки дают видимый ответ
- ✅ Нет errors в консоли
- ✅ TypeScript не ругается
- ✅ Нет дубликатов в данных

### Перед пуллом в main

- ✅ Напоминания работают
- ✅ Push-уведомления работают
- ✅ Голос говорит правильный текст
- ✅ История приёма актуальна

## 📝 РАЗМЕР ФАЙЛОВ

- ✅ Максимум 300 строк в одном файле
- ✅ Файлы более 400 строк — ошибка сборки
- ❌ НЕ создавайте гигантские файлы

## 🔐 БЕЗОПАСНОСТЬ

- Валидировать все пользовательские данные
- Санитизировать названия лекарств
- НЕ хранить чувствительные данные
- Проверять timezone и время

## 📱 PWA и APK

- Manifest.json должен быть валидным
- Service Worker должен кэшировать важные файлы
- Offline режим должен работать
- Подготовиться к TWA (Trusted Web Activity) для APK

---

**Последнее обновление:** 2026-04-19  
**Статус:** СТАБИЛЬНО - В СИЛЕ
